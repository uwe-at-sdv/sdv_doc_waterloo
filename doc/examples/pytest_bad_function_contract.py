def f_01() -> None:
	"""
	Preamble:
		profile:
			function
		normative_sections:
			Contract, Parameters, Returns, Raises
	Contract:
		general:
			|Must| demonstrate bad subsections in |label|`Contract`.
		requires:
			illegal_subsection:
				Subsection not allowed
	Parameters:
	Returns:
		|None|
	Raises:
	"""
def f_02() -> None:
	"""
	Preamble:
		profile:
			function
		normative_sections:
			Contract, Parameters, Returns, Raises
	Contract:
		general:
			|Must| demonstrate bad subsections in |label|`Contract`.
		ensures:
			illegal_subsection:
				Subsection not allowed
	Parameters:
	Returns:
		|None|
	Raises:
	"""
def f_03() -> None:
	"""
	Preamble:
		profile:
			function
		normative_sections:
			Contract, Parameters, Returns, Raises
	Contract:
		general:
			|Must| demonstrate bad subsections in |label|`Contract`.
		invariants:
			illegal_subsection:
				Subsection not allowed
	Parameters:
	Returns:
		|None|
	Raises:
	"""
