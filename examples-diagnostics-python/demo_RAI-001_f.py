def f(x: int) -> int:
	"""
	Preamble:
		profile:
			function
		normative_sections:
			Contract, Parameters, Returns
	Contract:
		general:
			|Must| work.
	Parameters:
		x:
			|Must| be an integer input.
	Returns:
		1
	"""
	return 1
